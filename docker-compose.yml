version: '3.8'

services:
  backend1:
    build: ./backend1
    ports:
      - "5001:5000"

  backend2:
    build: ./backend2
    ports:
      - "5002:5000"

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - backend1
      - backend2